<UserControl x:Class="SakuraLikeEditor.Views.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="LineNumbersColumn" Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border x:Name="LineNumbersBorder"
                Background="#F3F3F3"
                BorderBrush="#DDDDDD"
                BorderThickness="0,0,1,0">
            <ScrollViewer VerticalScrollBarVisibility="Hidden"
                          HorizontalScrollBarVisibility="Disabled"
                          Focusable="False">
                <TextBlock x:Name="LineNumbersTextBlock"
                           FontFamily="Consolas"
                           FontSize="14"
                           Margin="8,4,8,4"
                           TextAlignment="Right"
                           TextWrapping="NoWrap">
                    <TextBlock.RenderTransform>
                        <TranslateTransform x:Name="LineNumbersTransform" />
                    </TextBlock.RenderTransform>
                </TextBlock>
            </ScrollViewer>
        </Border>

        <TextBox x:Name="EditorTextBox"
                 Grid.Column="1"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 FontFamily="Consolas"
                 FontSize="14"
                 HorizontalScrollBarVisibility="Auto"
                 VerticalScrollBarVisibility="Auto"
                 SpellCheck.IsEnabled="False"
                 TextWrapping="Wrap"
                 Padding="8,4,8,4" />
    </Grid>
</UserControl>

