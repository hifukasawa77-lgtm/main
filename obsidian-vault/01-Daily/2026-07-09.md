---
type: daily
date: 2026-07-09
tags: [daily, self-improve, hook, lint]
---

# 2026-07-09

## 今日やったこと（/self-improve 11th）
- **recall hookの実バグを発見・修正**: 本セッションの想起が「直近Daily=06-28」と誤っていた（実最新は07-05）。原因はフレッシュクローン環境でのmtime同一化×`ls -t`。ファイル名日付比較へ修正 → [[0011-mtime-free-file-selection]]
- 同根バグを横串検出: `kpi-report.sh` の `find -newermt` も全件カウント化していたため同様に修正。harness-lint 検査#7（mtime依存ソート検出）を追加して恒久化（正・負テスト済み、SKILL.mdの検査一覧も同時更新）
- **Vaultの学びを昇格**（07-02/07-04/07-05分）:
  - CCR環境のgithub.io遮断→Actions「pages build and deployment」成否で代替確認 → `deploy-verify/SKILL.md`
  - Playwrightヘッドレスの `confirm()` 自動キャンセル・スクショのスクロール位置引き継ぎ → `dynamic-test/SKILL.md` 注意＋ `dynamic-tester.md` テンプレ（dialogハンドラ・scrollTo追加）
  - サブエージェント上限死＝作業消失ではない（ファイル実体確認→メイン引き継ぎ） → CLAUDE.md Code-Generator節
- [[harness-maintenance-patterns]] にmtime罠・コメント行誤検出の教訓を追記

## 学んだこと・気づき
- 想起（recall）自体が壊れていても気づきにくい。**hookの出力とVaultの実態の突き合わせ**は /self-improve の振り返り時に毎回やる価値がある（今回それで発見）。

## 意思決定
- [[0011-mtime-free-file-selection]]: hooks/skillsでのmtime依存ファイル選択を廃止し、lint検査#7で機械強制。

## 持ち越し事項の追跡調査（同日・続き）
- **34a148a（kotonoha公開整備）は既にmainへマージ済み**を確認 → 解消。
- **Garmin Data Sync失敗の原因判明**: リポジトリSecretsの `GARMIN_EMAIL` / `GARMIN_PASSWORD` が未設定（ログ上 env が空 → `fetch-garmin.js` が exit 1）。毎時cronで累計598ラン、直近30件すべて同一原因で失敗。`garmin-data.json` の最終更新は 2026-06-29 の手動コミット（9bf6e47）で、**ワークフロー経由の成功実績なしの可能性が高い**。加えて `scripts/fetch-garmin.js` 冒頭の `NODE_TLS_REJECT_UNAUTHORIZED='0'`（TLS検証無効化）は認証情報を扱うスクリプトとして要是正。対応方針は深澤の判断待ち（Secrets設定 or スケジュール停止 or 未設定時スキップ化）。
- `tmp-edge-profile-fahren` の履歴残存も再確認（混入コミットは 9bf6e47 / PR #203）。filter-repo＋パスワードローテーションは引き続き深澤の判断待ち。

## 追加調査・意思決定（同日・続き2）
- `tmp-edge-profile-fahren` の露出範囲を確定: リポジトリは**public**、履歴（9bf6e47）に `Login Data`・`Cookies` 含む223ファイルが残存（現mainツリーは除去済み）。**深澤判断: 履歴除去は今は保留**（main履歴書き換え＋force pushの影響を考慮）。公開状態が続くため、当該プロファイルのパスワードローテーション＋主要サイト再ログインは引き続き強く推奨。
- Garmin Data Sync: `workflow_dispatch` の手動トリガーはGitHub App権限で403（UIからのみ可能）。毎時cronの結果を12:28Zの自動チェックイン（send_later）で確認する運用に切替。
- 12:28Zチェックイン結果: 11:54Zの定期実行（マージ後main 44f5f3f）も「GARMIN_EMAIL / GARMIN_PASSWORD が未設定です」で失敗＝**Secrets未登録のまま**。15:30Z頃にもう1回だけ自動確認し、以降は深澤の登録連絡待ちとする。

## 次回への引き継ぎ（持ち越し）
- 【セキュリティ・保留中】`tmp-edge-profile-fahren` のgit履歴残存: 深澤判断で履歴除去は保留（2026-07-09）。パスワードローテーションのみ先行実施を推奨。実施指示が出たら filter-repo → force push → GitHub Supportキャッシュ削除依頼の順で対応。
- Garmin Data Sync: 深澤が「Secretsを設定して稼働」を選択。`fetch-garmin.js` のTLS検証無効化は除去し **PR #229 でmainマージ済み**（squash 44f5f3f、CI lint成功確認後）。残タスク: ①深澤がGitHub Secretsに GARMIN_EMAIL / GARMIN_PASSWORD を登録 ②workflow_dispatch手動実行で成功確認（登録の連絡を受けたらClaude側で実行可能）。
- ロト6同梱データの最新化（07-04、深澤のCSV入手待ち）。
