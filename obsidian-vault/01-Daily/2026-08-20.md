---
type: daily
date: 2026-08-20
tags: [daily, genpei, audit]
---

# 2026-08-20

## 源平争乱記: 総点検（バグなし）＋未使用アセット削除

深澤から「源平争乱記のバグ総点検」の依頼。既存の検査スクリプト3本
（`verify-genpei-boot.mjs` 全37項目・`verify-genpei-kyoten.mjs`・
`verify-genpei-balance.mjs` 5試行）に加え `verify-game-assets.mjs`・
a11y-audit・i18n-check・seo-audit をすべて実行し、いずれもPASS。
機能面のバグは見つからなかった。

手動でも要注意パターン（`sengoku.html`/`sanguo.html`/`taihei.html`で過去に
実際に踏んだ「肖像スロットのindexずれ」「アセットパスの部分一致」「論理座標と
画素座標の混同」等）を genpei.html に横展開して確認したが、該当するバグは
なかった。

### 見つけた1件: 未使用の肖像アトラス画像（バグではなく残骸）

`assets/genpei/portraits/*.webp`（102ファイル・456KB、`manifest.json`込み）が
コードからもドキュメントからも参照されていなかった。`genpei_基本設計.md` を
確認すると、当初は「肖像アトラス」方式（`assets/genpei/portraits/*.webp`）を
想定していたが、実装は個別AI生成画像方式（`assets/genpei/ai-portraits/*.png`、
`loadPortraits()` が `id + '.png'` で読む）に切り替わっており、旧設計時代の
アセットだけが削除されずに残っていた。GENERALS全50件の肖像は
`ai-portraits/` 側で過不足なく揃っていることも確認済み（Playwrightで
`GENPEI_DEBUG.GENERALS` のidと突き合わせ）。

削除してから `verify-genpei-boot.mjs` を再実行し、回帰がないことを確認して
`claude/genpei-souuran-ki-review-42lq0o` ブランチへコミット・プッシュした。

### ブラッシュアップ提案（未実装・深澤への提案のみ）

1. **モバイルの全国図ズーム/パン方式が sengoku/taihei に追いついていない**。
   genpei.html の全国図カメラはホイールズームと右/中ドラッグのみで、
   `taihei.html` が2026-08-19に追加したピンチズーム・1本指パン・
   フルスクリーン時の黒帯解消（`is-fullscreen` CSSフック）が genpei には
   移植されていない。スマホでの操作性に差が出ている。
2. `genpei_要件定義.md` の Could 項目（C-01〜C-08）がまだ全部未着手。
   特に C-03（名分の内訳可視化パネル）と C-01（一騎討ち戦績を列伝に表示）は
   実装コストが低く効果が見えやすい候補。
3. 上記2点は深澤の承認待ち。着手する場合は planner 経由で仕様化してから。
