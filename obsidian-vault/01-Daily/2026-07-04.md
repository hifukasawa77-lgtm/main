---
type: daily
date: 2026-07-04
tags: [daily, loto6, security, network]
---

# 2026-07-04

## 今日やったこと
- **ロト6確率統計アナライザー（loto6.html）を新規実装**（ブランチ: `claude/loto6-probability-analyzer-yoj7jz`）
  - 出現頻度/ホット&コールド/ギャップ/ペア共起ヒートマップ/合計値・奇偶・下一桁・連続数字（すべて理論値と対比表示）
  - 統計重み付き数字ピッカー3モード、CSV取込（Shift_JIS対応・列順自動判別）、手動追記、localStorage永続化
  - `loto6-data.js` に第1回〜第1197回の実データ同梱（Rosyuku/loto6 公開データセット、Python検証済み）
  - index.htmlカード＋app.js TRANS（ja/en）追加。Playwright全タブ検証PASS
- **セキュリティ是正**: `tmp-edge-profile-fahren/`（Cookies・Login Data含む223ファイル）がgit追跡されていたのを発見 → 追跡解除＋`.gitignore` に `tmp-edge-profile-*/` 追加

## 学んだこと・気づき
- **CCRリモート環境のネットワークポリシーは実質allowlist**（パッケージレジストリ＋raw.githubusercontent.com程度。WebFetchも例外なく403）。外部データ収集が要件のタスクは、着手前に到達可能性を確認し、公開GitHubデータセット等の代替経路を先に洗うこと
- 当選番号のような**事実データは著作権対象外**で同梱可。ただし鮮度が要件なら「同梱＋アプリ内取込で利用者更新」の2段構えが静的ホスティングの正解
- release-check が**過去コミット由来の問題**（追跡済みプロファイル）も検出してくれた。ゲートは新規変更以外too拾う前提で運用する

## 意思決定
- 同梱データは第1197回（2017-08）まで。最新化は後日 `loto6-data.js` 差し替え（深澤がCSV入手後）。アプリ側はCSV取込で利用者が自力更新可能
- Google GSI クライアント（`accounts.google.com/gsi/client`）はSRI付与不可（動的配信）。release-check #3 の既知例外として扱う

## 次回への引き継ぎ
- **【要対応・セキュリティ】** `tmp-edge-profile-fahren` は追跡解除したが**git履歴には残存**。リポジトリが公開なら `git filter-repo` 等で履歴からの完全除去＋当該プロファイルに保存されていたパスワードのローテーションを深澤に確認・実施すること
- ロト6の同梱データ最新化（第1198回〜最新）: KYO's LOTO6等からCSV入手 → `loto6-data.js` 再生成（変換スクリプトの形式: `[n,"YYYY-MM-DD",m1..m6,b]`）
