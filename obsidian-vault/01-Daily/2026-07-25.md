---
type: daily
date: 2026-07-25
tags: [daily, self-improve, sengoku, canvas]
---

# 2026-07-25

## 戦国風雲記: モーダル重なりと外交拡充
- **PR #281 拝領/処分モーダルの肖像と文字の重なり**: `_drawRetainerReaction` の「肖像画像なし（顔グラのみ）」分岐で、肖像だけ左寄せ（`px+100`）に配置する一方、名前・台詞・英訳はパネル全幅の中央揃え・`py+(img?400:140)` の上詰めYで描いていたため重なっていた。肖像をパネル中央（`px+pw/2-90, py+96`）へ寄せ、テキストのYを分岐側だけ 320/372/414 へ下げて解決。
- **PR #280 外交メニュー仕様変更**: 大名外交ページング／朝廷・幕府の詰み解消／武装勢力の友好度可視化。外交・処分・武将列伝の拡充、武将生没年と年代別配置、武将列伝と城CSV取込の改善。
- **学び（Canvasは片側分岐の更新漏れが即重なりになる）**: Canvasにはレイアウトエンジンが無く要素は互いを押しのけない。`py+(img?400:140)` 型の三項演算子で出し分けている箇所は、片方の分岐だけ座標を変えると他方が重なる。分岐したら同じパネル内の**全要素**の座標を洗い出し、分岐の全組み合わせでスクリーンショット検証する。

## /self-improve 16th（1週間分の未記録作業からの昇格）
- harness-lint 全検査 ✅。recall想起（直近Daily=2026-07-18.md）とVault実態の一致を確認 ✅。
- △警告2種を処理: ①07-18 のマーカー後追記2件（未昇格）②**07-19〜07-25 の7営業日が作業ありDailyなし**。
- 07-18 の2件を昇格: 疎通の事前確認 → agent-evolve スキル（`http_code=000` は worker障害でなく実行環境の外向き遮断。切り分けてPR提案欄へ上げる）／Vault追記のマーカー依存回避 → second-brain スキル書式ルール（`sed`のマーカー挿入は無音失敗するため末尾追記を基本に）。
- **7営業日分を遡及Daily化**（07-19〜07-25）。うち再利用可能な一般則を3系統・6項目、coding / game-dev スキルへ昇格 → [[0020-perf-cost-placement-and-canvas-layout-promotion]]。
- **学び（記録の空白は"別セッション作業"で起きる）**: 7日連続でDailyが無かったのは、戦国風雲記の実装が別ブランチ・別セッション（codex等）中心に進み、そのセッションが Vault へ書き戻さなかったため。recall hook は main の Vault しか見ないので、空白は次の /self-improve まで検知されない。harness-lint 検査#9（直近14日）が唯一の網なので、**/self-improve を週1で回す**ことが記録健全性の実質的な担保になる。

> ♻️ **昇格済み（2026-07-25, /self-improve 16th）**: Canvas分岐の座標整合は game-dev 新セクション「Canvas絶対座標レイアウトの整合」へ昇格済み。記録の空白の学びは運用頻度の話でハーネス指示への昇格対象外＝**昇格しない**（深澤へ週次運用として報告）。 → [[0020-perf-cost-placement-and-canvas-layout-promotion]]

## /self-improve 16th 続き（Routineドリフトの是正とループの週次自動化）
- **週次Routine化**: 16th で「/self-improve を週1で回すのが記録健全性の担保」と結論したため、深澤の了承を得て Routine を新設（毎週日曜21:00 JST / `0 12 * * 0` / `trig_01DuSy2rzQfPUcizikGGYJJg`）。固定ブランチ `claude/self-improve` のローリングPR方式、main直接push禁止、既存ルール変更は提案止まり。**MCPコネクタが引き継がれない警告**が出たため、GitHubツール不在時はブランチpushまでで正常終了する fallback を prompt に追加（初回firing 07-26 で要確認）。
- **1週間放置のドリフトを発見**: 07-18 に `/agent-evolve` を日次→週次へ変えたのは Routine の実態と当日Dailyだけで、CLAUDE.md・agent-evolve SKILL.md（6箇所）・guide-agent-evolution.md がすべて「毎日05:00」のまま残っていた。07-01昇格済みの「仕組みを変えたら説明文書まで更新対象」が適用されなかったケース。全10箇所を是正。
- **CLAUDE.md に「定期実行（Routine）一覧」表を新設**しスケジュールの正の単一ソースに。**harness-lint 検査#11** で表と各スキル記載の曜日・時刻を機械照合するようにした → [[0021-routine-schedule-single-source]]
- **学び（C localeでは多バイトのブラケット表現が空振りする）**: 検査#11の初回実装 `grep -oE '毎週[月火水木金土日]曜'` が全行抽出できず△を出した。`LANG` 未設定＝C locale ではバイト単位比較になるため。日本語を検査パターンに使うときはリテラルか `[^ ]` 等の単バイト否定クラスにする（`毎週[^ ]*曜`）。→ [[harness-maintenance-patterns]] の「bash 検査スクリプトの罠」へ追記済み。
- **学び（実態がAPI側にある設定は機械検証できない）**: Routineの実態はCIから読めないため、防げるのは「片方だけ更新」の内部ドリフトのみ。正を1箇所に集約し「そこを直せば検査が残りを指摘する」構図にするのが現実解。

> ♻️ **昇格済み（2026-07-25, /self-improve 16th 続き）**: C locale罠は [[harness-maintenance-patterns]] へ、Routine単一ソース化は CLAUDE.md＋harness-lint 検査#11＋self-improve SKILL.md 手順0へ反映済み → [[0021-routine-schedule-single-source]]

## /self-improve 16th 続き2（罠の横串展開とCI監視パスの穴）
- **C locale罠をコード側へ横串展開**: 「1件見つけたら横串で全件grep」を文書ではなくスクリプトに適用。`.claude/`・`scripts/`・`.github/` の27ファイルを走査した結果、多バイトブラケット表現の潜在インスタンスは**シェル側ゼロ**（唯一のヒット `scripts/gen_hyakunin_cards.js` はJS＝RegExpがUTF-16認識のため影響なし）。
- **harness-lint 検査#12を追加**: シェル検査スクリプトの多バイトブラケット表現を✗で検出。**負のテスト**（一時ファイルを置いて検出→削除して復帰）まで実施して空振りでないことを確認した。
- **CI監視パスの穴を発見・是正**: `.github/workflows/harness-lint.yml` は `paths: .claude/**` のみを監視していたが、検査#4/#5/#11が**正として読むのは `CLAUDE.md`**、検査#5の正は `accounting/budget.md`、検査#10の対象は `assets/js/agent-data.js`。つまり今日単一ソース化したRoutine表を書き換えてもCIが起動しない状態だった。監視パスへ4ファイル＋生成スクリプトを追加。`obsidian-vault/**` は検査#8/#9が警告のみ（exit codeに影響しない）ため意図的に除外。
- GitHub Actions は YAML アンカー（`&`/`*`）を解釈しないため、`paths` は push/pull_request に明示展開（その旨をワークフロー内にコメント）。
- **学び（検査の正が検査対象の外にあるとCIは無力）**: 「正の単一ソース化」と「CIの監視パス」はセットで設計する。正を別ファイルへ移した時点で監視パスを追随させないと、検査は存在するのに一度も走らない状態になる。
- **学び（壊れた正規表現は✗でなく偽の✓を出す）**: 検査ロジックのバグは失敗として現れず「通った」ように見えるため、lint自体の信頼性を静かに壊す。新しい検査を足したら必ず**負のテスト**（意図的に違反を作って✗が出るか）を実施する。

> ♻️ **昇格済み（2026-07-25, /self-improve 16th 続き2）**: 上記2件の学びは harness-lint 検査#12・CIワークフローの監視パス・self-improve SKILL.md 手順0へ反映済み → [[0021-routine-schedule-single-source]]

## /self-improve 16th 続き3（PR #282 マージ後: CI Action の非推奨解消）
- PR #282 を main へマージ（`32783b8`）。main の harness-lint CI は success、ログで**検査#11/#12がubuntu-latest上でも正しく動作**していることを確認（`毎週[^ ]*曜` はロケール非依存のため両環境で同結果）。
- マージ時のCI警告「Node.js 20 is deprecated（actions/checkout@v4）」に対応。4ワークフロー6箇所の `actions/checkout@v4` / `actions/setup-node@v4` を **@v7** へ更新。
- **学び（Actionのバージョンは推測せずタグを実確認する）**: 記憶で「最新はv5」と想定したが実態は **v7**（checkout v7.0.1 / setup-node v7.0.0）。推測でpinすると存在しないタグでCI即死か、古いまま放置になる。`git ls-remote --tags --refs` で確認し、メジャータグが実semverリリースと同一SHAを指すかまで見る。
- **学び（リリースノートが読めないなら action.yml を読む）**: GitHub API はこの環境でプロキシ403。`git clone --depth 1 --branch v7` して `action.yml` の `runs.using`（→ `node24`＝非推奨解消）と `inputs`（→ `node-version` 健在）を直接確認し、互換性を実証ベースで判断した。
- 使用箇所はすべて素の checkout（引数なし）と setup-node（`node-version` のみ）で、破壊的変更の影響を受けない形であることを確認済み。

> ♻️ **昇格済み（2026-07-25, /self-improve 16th 続き3）**: 上記2件の学びは workflow スキル新セクション「GitHub Actions の保守」へ昇格済み（CI監視パス・YAMLアンカー非対応の知見も同セクションへ集約） → [[0021-routine-schedule-single-source]]

## /self-improve 16th 続き4（Vault健全性とプロジェクトノートの陳腐化）
- PR #284 マージ済み（`7ebdaaf`）。CI実測で `checkout@v7` のSHAが事前確認値と一致し、**Node20非推奨の警告がログから消えた**ことを確認（全12検査✓）。
- **vault-gc 実行**: wikilink切れ・孤立ノート・Inbox滞留すべて✓。本日追加した ADR 0020/0021・遡及Daily 7本・多数のwikilinkに問題なし。harness-lint はハーネスを見るがVaultは見ないため、ノートを大量追加した日は vault-gc も回すこと。
- **知見ノート2本を点検**: `static-hosting-security-limits` `claude-md-project-rules` はいずれも昇格済みマーカーあり＝再昇格不要。
- **`second-brain-system.md` の陳腐化を発見・是正**: 「現在の状態: 2026-06-22 初期構築完了」のまま1ヶ月以上更新されず、その後の mtimeバグ修正・Daily圧縮ルール・週次Routine化が反映されていなかった。**本日3度目の「仕組みを変えたら説明文書まで更新対象」違反**（1度目=Routineスケジュール、2度目=CI監視パス）。履歴4件を追記し、TODOの「Daily書き込みは手動運用」に緩和状況（検知は機械化済み・残る穴は14日以上self-improveが回らない場合のみ）を追記。
- **学び（プロジェクトノートは"現在の状態"欄が腐る）**: ADRは出来事単位で追記されるが、プロジェクトノートの「現在の状態」は上書き更新が必要なため放置されやすい。ADRを起こしたら、関連するプロジェクトノートの状態欄にも1行追記するまでを1セットにする。

> ♻️ **昇格済み（2026-07-25, /self-improve 16th 続き4）**: 上記の学びは second-brain スキルの書式ルールへ昇格 → [[0021-routine-schedule-single-source]]
