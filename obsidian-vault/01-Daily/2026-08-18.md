---
type: daily
date: 2026-08-18
tags: [daily, taihei, mobile, canvas]
---

# 2026-08-18

## 太平風雲記: モバイル横画面ゲートを追加

- `taihei.html` はモバイル縦画面で 1440×810 (16:9) の Canvas が `max-width:96vw` だけで縦に大きく
  余白が空く不具合があった（横向き最適化ゲートが無かった）。スマホでタイトル画面を開くと
  上下に巨大な黒帯が出て、ボタンが画面中央の小さな帯にしか表示されない状態だった。
- `genpei.html` に既にあった「モバイル横画面ゲート」パターン（タッチ×縦画面のみ表示する
  `.landscape-gate` オーバーレイ＋ボタンで `requestFullscreen` → `screen.orientation.lock('landscape')`
  を試みる、失敗しても案内文自体で足りる設計）をそのまま `taihei.html` に移植した。
  `sengoku.html`/`sanguo.html` にも同種のゲートがあるが、genpei の実装が taihei と
  同じ GameKit.Engine(1440×810) 構成で一番近かったため、そちらを踏襲した。

### 学び（TAIHEI_DEBUG ブリッジの追加漏れ）

検査スクリプト（`verify-taihei-boot.mjs`、新設）を書く際、`window.TAIHEI_DEBUG.buildState` を
直接呼ぼうとして `no-buildState` で落ちた。実際の実装は `buildState` がトップレベル関数ではなく
`Rule.buildState`（`Rule = { ..., buildState, ... }` にまとめられている）で、`Rule` 自体は
ブリッジに載っていた。**ブリッジの記載漏れではなく検査側の呼び出し誤り**だったが、
「トップレベル関数だと思い込んで直接呼ぶ」は sanguo/sengoku 検査でも起こり得る典型的な
誤診パターンなので、次に検査を書くときはまずブリッジのキー一覧を確認してから関数を叩くこと。

### 動的検証

`node scripts/verify-taihei-boot.mjs` を新設。ゲート表示条件（縦画面でタッチ端末のみ表示・
横画面/非タッチで非表示）に加え、以下も Playwright で実機検証し、`game.errors`
（GameKitがフレーム内で捕捉した例外）も合算して判定する:
- 起動〜陣営選択〜オープニング〜MapScene〜ターン終了1回
- 隣国への出兵→BattleSceneでヘックス合戦を最大10ラウンド消化→MapSceneへ帰還
- 3陣営（足利方・南朝方・地方勢力1家=大内氏）それぞれ最大65ターン自動進行させ、
  CutsceneScene（年代記イベント演出）・EndingScene（南北朝合一エンディング）到達を確認
  （南朝方・大内氏は17ターンでエンディング到達、足利方は59ターンでも未到達だが例外なし）

全項目PASS、例外0件。ゲームロジック・シーン遷移側に「仕様的におかしな箇所」は見つからなかった。

### 検討したが見送った項目（モバイルのタップ判定サイズ）

MapScene のコマンドバー等はデザインキャンバス 1440×810 に固定座標で配置されており、
iPhone 13横画面（実測キャンバス幅588 CSS px、スケール率0.41）では
ボタン高さが 52canvas-px × 0.41 ≈ 21 CSS px 相当まで縮む。これは `sengoku.html`/`sanguo.html`
が `UI_SCALE`／パネル拡大＋入力逆変換で対処した「固定Canvas UIのモバイル可読性対策」
（`.claude/skills/game-dev/SKILL.md`）と同種の症状ではある。
ただし姉妹作 `genpei.html`（同じ1440×810構成で横画面ゲートも既にある）も同種のUI_SCALE機構を
持たないまま運用されており、本リポジトリの現行基準では「横画面ゲートの新設」に
このスケーリング対応まで含めるのが必須という前例はないため、今回は見送った。
将来 taihei.html でモバイル操作性の指摘が出た場合は、sengoku.html の
`computeUiScale()`／パネル拡大＋逆変換パターンを移植する対応を検討する。

## 太平風雲記: タイトル文字を毛筆体(Yuji Boku)へ

- タイトル画面の「太平風雲記」（Canvas `fillText`）を、深澤の依頼で草書体風にした。
  本サイトはフレームワーク不使用・CDN経由でライブラリ追加という方針（CLAUDE.md）だが、
  和文の真の草書体（続け書き・大胆な省略）をカバーするWebフォントはGoogle Fontsに無く、
  最も近い選択肢は「Yuji Boku」（Zen宗の墨跡＝bokuseki に着想を得た毛筆体）だった。
  比較のため Yuji Syuku / Klee One / Zen Kurenaido も実フォントを取り寄せて見比べ、
  最も毛筆の擦れ・撥ねが強く出る Yuji Boku を採用した。
- CDN: `<link href="https://fonts.googleapis.com/css2?family=Yuji+Boku&display=swap">` を追加。
  Canvasテキストは読み込み前に描画するとフォールバック書体でちらつくため、
  `document.fonts.load()` を `BootScene` の `Promise.all([dataReady, loadMapImage(), fontReady])`
  に合流させ、`TitleScene` に遷移する前に確実に読み込みを終えるようにした
  （失敗時は `.catch(()=>null)` でフォールバック書体のまま進行し、ブロックしない）。
- 大きい見出し（64→76px、字間の全角スペース除去）は毛筆体向けに調整。連続する字間の
  空白は明朝体では字間調整として機能するが、毛筆体では字と字が離れすぎて流れが切れるため外した。

### 学び（このサンドボックスはブラウザから外部HTTPSへ直接張れない）

`curl -x $HTTPS_PROXY --cacert /root/.ccr/ca-bundle.crt` は正しく検証つきで通るが、
PlaywrightのヘッドレスChromiumは `HTTPS_PROXY` 環境変数を自動では読まず、明示的に
`proxy:{server}` を渡してもこの環境では `fonts.googleapis.com` 宛だけ `ERR_CONNECTION_RESET`
になった（`www.google.com` 等はプロキシのポリシー拒否として記録されるのに対し、
`fonts.googleapis.com` 宛はプロキシのアクセスログに一切現れない＝プロキシへ到達する前に
ブラウザ側で弾かれている）。`--ignore-certificate-errors` で回避するのは
「TLS検証を無効化しない」という明示ルールに反するため使わず、代わりに
`curl`（プロキシのCA経由で正規に検証）でフォント本体を取得し、Playwrightの
`page.route()` でその実バイトを代理応答することで、**コミットするHTMLは変更せずに**
実物のフォントでのレンダリングを検証した。次に同種（Canvas上のWebフォント検証）が
必要になったら、まず `page.route` 代理応答の方法を使うこと（ブラウザ直の外部HTTPSは
このサンドボックスでは前提にしない）。

## 太平風雲記: ブラッシュアップ（監査→視覚バグ修正・毛筆体展開・モバイル操作性・バランス診断）

深澤の依頼「ブラッシュアップがしたい」に対し、監査してから着手する方針で実施。

### 視覚バグ修正
- 悪党パネルの4コマンド（夜襲/放火/略奪/城門破壊）で、英語ラベルと「基礎成功率XX%」の
  テキストがほぼ同じy座標に描かれて重なり合っていた（`button()`のen位置=`h/2+6`、
  成功率位置=`h-14`が4px しか離れていなかった）。ボタン高さ48→58px・行間58→68pxへ拡大し解消。

### 毛筆体(Yuji Boku)を見出し全般へ展開
- FactionSelectScene「陣営を選ぶ」・OpeningScene「元弘の乱」・EndingScene結末タイトル・
  CutsceneSceneの年代記見出しに展開。
- **展開中に新しい表示バグを発見**: Yuji Bokuは半角数字"1"を"7"に似た崩し字形で描くため、
  CutsceneSceneの見出し「【1331年】元弘の乱」が「【7337年】元弘の乱」に見えてしまっていた
  （実データは正しく、フォントのグリフ形状だけの問題）。歴史シムで年号の誤読は致命的なため、
  **数字を含む見出し（CutsceneSceneのみ）だけMincho体へ差し戻した**。6エンディング全タイトル・
  FactionSelect・Openingの見出しは元々数字を含まないため毛筆体のまま問題なし。
  → 学び: 装飾フォントを見出しへ展開するときは「その文字列に算用数字が混じらないか」を
  必ず確認すること。見た目の綺麗さだけで判断すると実際にブラウザで数字を描画するまで気づけない。

### モバイル操作性（コマンドバーのタップ判定拡大）
- 前日見送った項目に着手。`ML`（MapSceneの固定レイアウト定数）で map/side を560→544px、
  log/cmdのy座標を16px分前倒しし、その分をcmdバーの高さ72→88pxへ回した
  （ボタン実高さ 52→68px、+30%）。画面全体の縦合計・パネル間の余白(8px)は変更前と同一に
  保ち、デスクトップ・モバイル共通の1レイアウトのまま調整（sengoku.html流の「タッチ限定の
  パネル拡大＋入力逆変換」は、本ページの規模では過剰と判断し見送った）。

### バランス診断（game-balanceエージェント、コード変更なし・分析のみ）
前日「南朝方・大内氏は17ターン、足利方は59ターンでもエンディング未到達」という差の原因を
特定してもらった。**構造的なバグ**と判明:
- `aiTurn`の「朝廷工作」ステップが、`court`を持つ**全AI陣営に無条件で**`courtPropose(kani)`を
  実行し、成否に関わらず`courtRel += 5/ターン`する。
- 足利方がAI操作（＝プレイヤーが足利方以外を選択）のとき、このロジックが足利方自身の
  対南朝courtRelを毎ターン+5〜6.5押し上げ、史実イベント「中先代の乱」の離反条件
  （`courtRel < 25`）をturn4より前に潰してしまう。結果、①建武の新政成功が
  `kenmuStartTurn(2)+kenmuHoldTurns(15)=turn17`で機械的に確定する。
- 逆に足利方がプレイヤー操作のときはAIが動かないため離反が史実通りturn4で発火し①が
  永久ブロックされるが、**AIには出兵・国盗りロジックが一切ない**ため②③④⑥（石高/制圧国数
  条件のエンディング）も到達不能で、⑤かフォールバック（turn61）まで押し込まれる。
- 提案（優先度順、いずれも未着手・要承認）: P1 足利方×court==nancho×離反前は朝廷工作を
  スキップする分岐を追加（根本原因）／P2 マジックナンバー（離反閾値25・官位工作+5）を
  `RULE`へ定数化／P3 P1後に`kenmuHoldTurns`を再調整／P4 AIに最低限の侵攻ロジックを追加
  （②③④⑥エンディングの到達可能化、設計要件からの新規機能なので別途Planner起票が妥当）。
- 深澤に提示し、P1（根本原因の小さな分岐追加）を実施する方針で承認を得た。P2〜P4は見送り。

### P1修正の実施と効果検証

`aiTurn`の朝廷工作ステップに `campId==='ashikaga' && camp.court==='nancho' && !state.flags.ashikagaBreak`
の間はスキップするガードを追加（`taihei.html` L1222付近）。`Rule.endTurn`を65回直接叩く
ヘッドレス実測で効果を確認:

| playerCamp | 修正前 turn4のcourtRel(足利,南朝) | 修正後 turn4のcourtRel | 修正後ashikagaBreak | 修正後の帰結(65ターン以内) |
|---|---|---|---|---|
| nancho | 26 | **4.5** | turn4でtrue | scenario_end_fallback(turn61) |
| ashikaga | 0（元々AI停止） | 4.5（変化なし、想定通り） | turn4でtrue | scenario_end_fallback(turn61) |
| ouchi | 26 | **4.5** | turn4でtrue | **tenka_taihei(turn61)** |

3陣営とも中先代の乱がturn4で史実通り発火するようになり、「陣営選択だけでturn17固定エンドが
機械的に決まる」非対称は解消。ouchi陣営はP1修正後に史実イベントの連鎖を経て天下泰平エンド
（南北朝合一、turn61）へ到達する変化も確認できた。`verify-taihei-boot.mjs`のロングラン検査は
カットシーン送りクリックも予算を消費する都合で、旧予算65では一部陣営がturn61到達前に
打ち切られていたため90へ拡大した。3陣営とも例外0件・turns58〜59・cutscenes6〜7件で
挙動が揃うことを確認（Playwright実クリックでのカットシーン送りは1件あたり最大30クリックの
安全弁があり、65→90への拡大で全陣営が同程度の深さまで到達するようになった）。

### 学び（バランス系の"体感差"は数値ではなくロジック分岐を疑う）

「陣営によってエンディング到達ターン数が3倍違う」という報告を受けた時点では単なる
数値バランスの偏りを疑いがちだが、今回は**AIの汎用ヒューリスティックが特定陣営専用の
史実トリガー条件を意図せず踏み潰す**という設計上の相互作用バグだった。数値だけを
いじる前に「その数値を動かしている経路は本当に1つか（他のAIロジックが同じ状態変数を
横から動かしていないか）」を確認する価値がある。

## 前日の記録

- 2026-08-17 に戦国風雲記の拠点グラフィックを倍率別に調整。詳細は [[0025-csv-sync-direction-and-newline-preservation]] と `01-Daily/2026-08-17.md`。

_(新しい学び・決定事項は obsidian-vault/ に追記する。詳細は .claude/skills/second-brain/SKILL.md を参照)_

---
> ♻️ **昇格済み（2026-08-28, /self-improve）**: 「デバッグブリッジを叩く前にキー一覧を確認」は `coding` スキルへ、「サンドボックスの外部HTTPS検証（page.route代理応答）」は `dynamic-test` スキルへ、「装飾フォントの算用数字グリフ確認」は `design` スキルへ、「体感差はロジック分岐を疑う」は `game-balance` エージェント定義へ反映済み。視覚バグ修正・モバイル操作性拡大・P1修正自体は一回限りの実装作業のため昇格しない。
