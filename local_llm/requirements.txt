# 自作ローカルLLM用の依存パッケージ
# Windows（GPUなし）では torch はCPU版を推奨:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
sentencepiece>=0.2.0
numpy>=1.26.0
datasets>=2.18.0
fastapi>=0.110.0
uvicorn>=0.29.0
